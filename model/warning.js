/******************************************************************************************************
 * Objetivo: Arquivo responsavel pelo CRUD referente a tabela de NOTIFICACAO da aplicação SyncroBaby
 * Data: 24/04/2026
 * Autor: SyncroBaby
 * Versão: 1.0
 * ****************************************************************************************************/

//Criar variavel para conexao com o banco

const getWarningByUserId = async function (id_user) {
    try {
        //Script sql
        let sql = null

        //Variavel de encaminhamento ao banco
        let result = null

        if (Array.isArray(result))
            return result
        else
            return false

    } catch (error) {
        return false
    }
}

const getWarningByChildrenId = async function (id_children) {
    try {
        //Script sql
        let sql = null

        //Variavel de encaminhamento ao banco
        let result = null

        if (Array.isArray(result))
            return result
        else
            return false

    } catch (error) {
        return false
    }
}

const setDeleteUser = async function (id) {

}